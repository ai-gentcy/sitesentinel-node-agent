# Copy to /etc/sitesentinel/agent.env. No token needed: on first start the
# agent generates its own key and logs "Registration hash: …" — add that hash
# in the backoffice (Nodes -> Add node) to activate the node.
SENTINEL_URL=https://nodes.sitesentinel.io
